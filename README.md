# Ada-SPARK-Word-Search-II-Lite

A small bounded horizontal-word-search variant for a fixed board. Implemented as a small bounded Ada SPARK example with `SPARK_Mode => On`.

```sh
make test
make prove
```
